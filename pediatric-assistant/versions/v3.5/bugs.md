# 版本 v3.5 Bug 记录

> **版本**: v3.5
> **发布日期**: 2026-02-13
> **状态**: 开发中

---

## 一、已知问题

### BUG-V35-001: 微信浏览器 beforeunload 事件不触发

**严重度**: 🟡 中

**状态**: ⚠️ 待修复

**描述**:
在微信内置浏览器中，`window.beforeunload` 事件无法可靠触发，导致用户关闭页面前无法显示归档提示。

**复现步骤**:
1. 在微信中打开应用
2. 创建对话并发送消息
3. 点击微信左上角返回按钮
4. 观察是否显示确认对话框

**预期结果**:
- 显示"您有未归档的对话，确定要离开吗？"

**实际结果**:
- 无提示，直接退出

**根因分析**:
- 微信浏览器对 `beforeunload` 事件有限制
- 部分 iOS 和 Android 版本行为不一致

**临时解决方案**:
- 依赖30分钟计时器提醒
- 在页面显著位置添加"保存"按钮

**修复方案**:
1. 添加 `visibilitychange` 事件监听
2. 页面切换到后台时显示归档提示（通过微信 API）
3. 添加"保存提醒"按钮作为备选

**优先级**: 中（不影响核心功能，但影响用户体验）

**计划修复时间**: v3.5.1

---

### BUG-V35-002: 30分钟计时器在页面后台运行时不准确

**严重度**: 🟢 低

**状态**: ⚠️ 待修复

**描述**:
当用户将页面切换到后台（如切换到其他标签页或最小化窗口），浏览器会降低计时器执行频率，导致30分钟计时器触发时间不准确。

**复现步骤**:
1. 创建对话
2. 将页面切换到后台30分钟
3. 切换回前台
4. 观察计时器触发时间

**预期结果**:
- 30分钟后准确触发提醒

**实际结果**:
- 触发时间延迟，可能延迟1-5分钟

**根因分析**:
- 浏览器对后台标签页的 `setTimeout` 执行频率有限制
- Chrome/Safari 会降低后台页面的计时器精度

**临时解决方案**:
- 用户切换回前台时会触发提醒（虽然延迟）

**修复方案**:
1. 使用 `visibilitychange` 事件检测页面可见性变化
2. 页面切换回前台时重新计算时间差
3. 如果超过30分钟，立即显示提醒

**优先级**: 低（影响有限，用户仍能收到提醒）

**计划修复时间**: v3.5.1

---

### BUG-V35-003: 多次快速点击归档按钮导致重复请求

**严重度**: 🟢 低

**状态**: ⚠️ 待修复

**描述**:
用户快速连续点击"归档"按钮时，可能发送多个归档请求，导致后端重复处理。

**复现步骤**:
1. 创建对话
2. 快速连续点击"归档"按钮3次
3. 观察网络请求

**预期结果**:
- 只发送1个归档请求

**实际结果**:
- 发送多个请求（取决于点击速度）

**根因分析**:
- 归档按钮没有防抖处理
- 归档流程是异步的，第一次点击未完成前可以再次点击

**临时解决方案**:
- 后端幂等性保证：重复归档同一对话不会出错

**修复方案**:
1. 在归档流程开始时禁用归档按钮
2. 归档完成后重新启用按钮
3. 添加防抖处理（300ms）

**优先级**: 低（后端已有幂等性保护）

**计划修复时间**: v3.5.1

---

### BUG-V35-004: mark_archived() WHERE clause 错误导致归档失败

**严重度**: 🔴 高

**状态**: ⚠️ 待修复

**描述**:
`conversation_service.py` 的 `mark_archived()` 方法在 line 409 的 WHERE 子句中错误地使用 `user_id = ?` 并绑定了 `member_id`，导致归档操作始终失败。

**位置**:
`backend/app/services/conversation_service.py:409`

**错误代码**:
```python
conn.execute(
    """
    UPDATE conversations
    SET archived = 1, archived_at = ?, archived_member_id = ?
    WHERE id = ? AND user_id = ?
    """,
    (now, member_id, conversation_id, member_id)  # ❌ 最后的 member_id 应该是 user_id
)
```

**问题**:
- WHERE 子句检查 `user_id = member_id`
- member_id 是 "member_baby_001" 这样的成员 ID
- user_id 是 "user_abc123" 这样的用户 ID
- 两者永远不匹配，导致 UPDATE 失败

**修复方案 1** (推荐):
移除 `user_id` 检查，只用 `conversation_id` 定位记录
```python
WHERE id = ?
```

**修复方案 2**:
修改方法签名，接受 `user_id` 参数
```python
def mark_archived(self, conversation_id: str, member_id: str, user_id: str) -> bool:
    # ...
    WHERE id = ? AND user_id = ?
    # 绑定: (now, member_id, conversation_id, user_id)
```

**影响范围**:
- E2E 测试全部失败（9/10 个场景）
- 用户无法归档对话到健康档案

**优先级**: 高（核心功能完全不可用）

**计划修复时间**: 立即修复

---

### BUG-V35-005: get_user_conversations() 缺少 archived 字段

**严重度**: 🟡 中

**状态**: ⚠️ 待修复

**描述**:
`get_user_conversations()` 方法的 SELECT 语句没有包含 `archived` 和 `archived_member_id` 字段，导致前端无法判断对话是否已归档。

**位置**:
`backend/app/services/conversation_service.py:205-210`

**错误代码**:
```python
SELECT id, title, message_count, created_at, updated_at
FROM conversations
WHERE user_id = ?
```

**问题**:
- 缺少 `archived` 字段
- 缺少 `archived_member_id` 字段
- 缺少 `archived_at` 字段

**修复方案**:
```python
SELECT id, title, message_count, created_at, updated_at,
       archived, archived_member_id, archived_at
FROM conversations
WHERE user_id = ?
```

同时更新返回的字典 (line 214-220):
```python
{
    "conversation_id": row["id"],
    "title": row["title"],
    "message_count": row["message_count"],
    "created_at": row["created_at"],
    "updated_at": row["updated_at"],
    "archived": row["archived"],
    "archived_member_id": row["archived_member_id"],
    "archived_at": row["archived_at"],
}
```

**影响范围**:
- 前端侧边栏无法显示归档状态
- E2E 测试失败
- 用户无法区分已归档和未归档对话

**优先级**: 中（功能部分不可用）

**计划修复时间**: 立即修复

---

## 二、已修复问题

### BUG-V35-004: localStorage 清理后无法重新登录

**严重度**: 🔴 高

**状态**: ✅ 已修复（2026-02-13）

**描述**:
当用户清除 `localStorage` 后，重新登录时无法正确保存 `user_id`。

**复现步骤**:
1. 清除浏览器 `localStorage`
2. 刷新页面
3. 输入邮箱登录
4. 刷新页面
5. 观察是否需要重新登录

**预期结果**:
- 第二次刷新时自动登录

**实际结果**:
- 第二次刷新时仍显示登录遮罩层

**根因分析**:
- `handleLoginSubmit()` 中 `localStorage.setItem()` 在异步回调后执行
- 部分情况下可能在页面跳转前未完成

**修复方案**:
- 使用 `await` 确保 API 调用完成后再保存 `user_id`
- 添加 fallback 逻辑，API 失败时仍保存本地数据

**修复代码**:
```javascript
async function handleLoginSubmit() {
  try {
    const response = await fetch(`${API_BASE}/api/v1/auth/register`, {
      method: 'POST',
      body: JSON.stringify({ user_id: generatedUserId })
    });

    if (response.ok) {
      const data = await response.json();
      localStorage.setItem('pediatric_user_id', data.data.user_id);
    } else {
      // Fallback
      localStorage.setItem('pediatric_user_id', generatedUserId);
    }
  } catch (error) {
    // Fallback
    localStorage.setItem('pediatric_user_id', generatedUserId);
  }

  await loadConversations();
}
```

**验证**: ✅ 已验证修复

---

### BUG-V35-005: 归档对话后侧边栏未刷新

**严重度**: 🟡 中

**状态**: ✅ 已修复（2026-02-13）

**描述**:
归档对话成功后，侧边栏仍显示该对话为活跃状态，未显示"已归档"标签。

**复现步骤**:
1. 创建对话
2. 点击"归档"按钮
3. 归档成功
4. 观察侧边栏

**预期结果**:
- 侧边栏显示📁图标和"已归档"标签

**实际结果**:
- 侧边栏无变化，仍显示为活跃对话

**根因分析**:
- 归档成功后未调用 `loadConversations()` 刷新侧边栏

**修复方案**:
- 在 `performArchive()` 函数中归档成功后调用 `await loadConversations()`

**修复代码**:
```javascript
async function performArchive(convId, memberId) {
  const response = await fetch(`${API_BASE}/api/v1/conversations/${convId}/archive`, {
    method: 'POST',
    body: JSON.stringify({ member_id: memberId, user_id: CURRENT_USER_ID })
  });

  if (!response.ok) throw new Error('归档失败');

  showBanner("对话已归档到健康档案", "info");
  conversationId = null;
  chat.innerHTML = "";

  // 🔧 修复：重新加载对话列表
  await loadConversations();
}
```

**验证**: ✅ 已验证修复

---

## 三、待测试问题

### ISSUE-V35-001: Safari 浏览器登录输入框自动填充

**严重度**: 🟢 低

**状态**: 🔍 待验证

**描述**:
Safari 浏览器可能自动填充邮箱地址到登录输入框，但前端生成的 `user_id` 可能与用户预期不符。

**测试步骤**:
1. 在 Safari 中打开应用
2. 观察登录输入框是否自动填充
3. 验证自动填充的邮箱生成的 `user_id` 是否正确

**待验证**:
- Safari 自动填充行为
- 自动填充后 `user_id` 生成是否正确

**优先级**: 低

---

### ISSUE-V35-002: 归档对话加载性能

**严重度**: 🟢 低

**状态**: 🔍 待验证

**描述**:
当用户有大量归档对话（100+）时，侧边栏加载速度可能变慢。

**测试步骤**:
1. 创建100个归档对话
2. 刷新页面
3. 测量侧边栏渲染时间

**待验证**:
- 侧边栏渲染性能
- 是否需要分页加载

**优先级**: 低（当前用户数量少，暂无此问题）

---

## 四、风险评估

### 4.1 高风险问题

无

### 4.2 中风险问题

- **BUG-V35-001**: 微信浏览器归档提示（影响用户体验）

### 4.3 低风险问题

- **BUG-V35-002**: 计时器精度（影响有限）
- **BUG-V35-003**: 重复点击（后端已保护）

---

## 五、修复计划

### v3.5.1 (计划发布: 2026-02-20)

- [ ] 修复 BUG-V35-001: 微信浏览器归档提示
- [ ] 修复 BUG-V35-002: 计时器精度优化
- [ ] 修复 BUG-V35-003: 归档按钮防抖

### v3.5.2 (计划发布: TBD)

- [ ] 优化归档对话加载性能（如有需要）

---

## 六、测试建议

### 6.1 回归测试

- 每次修复 Bug 后运行完整的 E2E 测试套件
- 验证 392 个现有测试用例仍全部通过

### 6.2 兼容性测试

- 重点测试微信浏览器（iOS 和 Android）
- 测试 Safari 自动填充行为

### 6.3 性能测试

- 测试大量归档对话场景（100+）
- 监控侧边栏渲染时间

---

*最后更新: 2026-02-13 by Agent C*
